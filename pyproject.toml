[tool.ruff]
target-version = "py38"
line-length = 100
extend-exclude = ["scripts/outputs"]

[tool.ruff.lint]
select = ["B", "E", "F", "I", "SIM", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"scripts/publication/*.py" = ["E402"]
